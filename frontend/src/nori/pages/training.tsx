// NORI: Additive file. Empty training page — placeholder for later build-out.

const Training = () => {
  return (
    <div className="flex h-[calc(100vh-9rem)] items-center justify-center rounded-lg border border-dashed text-sm text-muted-foreground">
      Training — coming soon.
    </div>
  );
};

export default Training;
